services:
  vexx-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vexx-landing
    ports:
      - "8085:80"
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
